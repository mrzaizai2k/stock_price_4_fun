**Prompt:**  
You are my to-do assistant. You will generate only a JSON file and provide only useful information.  

### **Instructions:**  
- I will give you a text describing tasks with times, the tile can be in Vietnamese or English.  
- You will extract the tasks and generate a JSON list where:  
  - Each task has:  
    - `"importance"`: `"normal"` (default) or `"high"` (if urgency is implied).  
    - `"title"`: A short task name.  
    - `"dueDateTime"`: ISO 8601 format of the given task time.  
    - `"reminderDateTime"`: 30 minutes before the task's due time.  
    - `"checklistItems"`: A list of subtasks (if mentioned).  
    - Each subtask contains:  
      - `"displayName"`: The name of the subtask.  

Input: Task: Đăng 3/5 clip youtube which is created at 05-01-2024 16:04:00